---
name: workflow-status
description: Inspect canonical workflow status from shared repository artifacts.
---

Use `workflow/scripts/phase-status.sh` and `workflow/scripts/tasks-sync.sh --dry-run` to summarize canonical status.

If the investigation is broad, prefer `@opsx-reviewer` for a scoped analysis pass.
