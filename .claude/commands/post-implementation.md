# Legacy Wrapper: `/post-implementation`

Use `/opsx/verify` as the canonical flow.

Canonical evidence belongs in:

- `openspec/changes/<change-id>/verification.md`
- `workflow/state/status.md`
- `workflow/state/reports/`
