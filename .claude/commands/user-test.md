# Legacy Wrapper: `/user-test`

Use the active OpenSpec change and canonical workflow state.

- store live verification needs in `workflow/state/pending-usertests.md`
- write durable change-level evidence in `openspec/changes/<change-id>/verification.md`
- store execution reports in `workflow/state/reports/`
