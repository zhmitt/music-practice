# Legacy Wrapper: `/spec-review`

Use `/opsx/verify` as the canonical review and verification entry point.

Perform review against the active change in `openspec/changes/<change-id>/` and write resulting evidence into canonical files.
