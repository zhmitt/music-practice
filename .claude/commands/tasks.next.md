# Legacy Wrapper: `/tasks next`

Use `/opsx/status` plus `workflow/state/roadmap.md` as the canonical decision surface.

If there is no active change, the next step is to activate one in `openspec/changes/`.
