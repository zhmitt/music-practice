---
description: Inspect canonical workflow status.
argument-hint: optional change id
---

Use `workflow/scripts/phase-status.sh`, `workflow/scripts/tasks-sync.sh --dry-run`, and `workflow/state/` to summarize the repository state.

