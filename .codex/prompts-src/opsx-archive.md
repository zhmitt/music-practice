---
description: Archive a fully verified OpenSpec change.
argument-hint: change id
---

Archive only after the canonical completion gate passes. Prefer `openspec archive <change-id>` when available, then refresh workflow state.

