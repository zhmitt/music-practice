---
description: Implement work from an active OpenSpec change.
argument-hint: change id
---

Read the active change and `workflow/state/task-registry.md`.

Implement from `tasks.md`, keep canonical artifacts current, and refresh deterministic state with `workflow/scripts/tasks-sync.sh`.

